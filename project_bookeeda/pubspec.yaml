name: project_bookeeda
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  flutter_native_splash: ^2.3.2
  cupertino_icons: ^1.0.6
  smooth_page_indicator: ^1.0.0 # Specify a version for smooth_page_indicator
  carousel_slider: ^4.0.0
  cached_network_image: ^3.3.1
  google_fonts: ^6.1.0
  motion_tab_bar: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true

  assets:
    - assets/book_images/
